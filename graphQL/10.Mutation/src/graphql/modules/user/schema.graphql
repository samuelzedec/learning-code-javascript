type Usuario {
    id: ID
    nome: String
    email: String
    telefone: String
    perfil: Perfil
}

type Query {
    usuario(id: ID): Usuario
    usuarios: [Usuario]
}

#Palavra reservada
type Mutation {
    criarUsuario(nome: String, email: String, telefone: String) : Usuario!
}

